[package]
name = "coremark-openvm"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
openvm = { git = "https://github.com/openvm-org/openvm.git", tag = "v1.4.3", features = ["std"] }

[build-dependencies]
cc = "1.2.54"
