[package]
name = "openvm-coremark"
version = "0.1.0"
edition = "2021"
build = "build.rs"
license = "MIT"

[dependencies]
openvm = { git = "https://github.com/openvm-org/openvm.git", tag = "v2.0.0", features = ["std"] }

[build-dependencies]
cc = "1.2.54"
